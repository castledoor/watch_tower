
<script type="text/JavaScript">
function timedRefresh(timeoutPeriod) {
	setTimeout("location.reload(true);",timeoutPeriod);
}
// </script>
<body onload="JavaScript:timedRefresh(10000);">
	<div class="container">
		<div class="picture">
	    <p> WATCH TOWER </p>
            <% if no_location = false %>
				<%= image_tag(@photo.url, :class => "instagram") %>
				<%= @image.geo_code_city %>
			<% elsif no_location = true %>
				<%= image_tag(@photo.url, :class => "instagram") %>
			<% end %>
		</div>
	</div>
</body>











